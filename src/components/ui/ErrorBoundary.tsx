/**
 * Re-exports the canonical ErrorBoundary to avoid duplication.
 * All new code should import from '@/components/ErrorBoundary' directly.
 */
export { ErrorBoundary as default } from '@/components/ErrorBoundary';
