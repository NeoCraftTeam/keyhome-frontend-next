/**
 * Unified nav icon metrics (PWA bottom tabs, mobile drawers, owner sidebar).
 * Matches MUI BottomNavigation shell: `.MuiSvgIcon-root { fontSize: '1.5rem' }` (24px).
 */
export const NAV_LIST_ICON_GLYPH_PX = 24;

/** Client `NavDrawer` / MUI list — label column aligns when this is fixed */
export const CLIENT_DRAWER_LIST_ICON_MIN_WIDTH_PX = 48;

/** Owner desktop sidebar — slightly narrower rail than client drawer */
export const OWNER_SIDEBAR_LIST_ICON_MIN_WIDTH_PX = 40;
