/**
 * Vertical space reserved below main content when the native-style bottom tab bar
 * is visible (installed PWA / standalone display mode only — not mobile browser tabs).
 *
 * Matches the `BottomNavigation` `height` minus the OS safe-area inset (added in sx).
 */
export const PWA_BOTTOM_NAV_INNER_HEIGHT_PX = 72;
